<% var dataList = albumList; %>
<% var data; %><!doctype html>
<html>
  <%- include('partial/_head'); %>
  <body>
    <%- include('partial/_nav', {
      current: page.name
    }); %>
    <main>
      <div class="container-fluid my-5">
        <div class="row">
          <div class="col-12">
            <div class="card-columns-ex">
              <% for (var i = dataList.length - 1; i >= 0; i--) { %>
              <% data = dataList[i]; %>
              <div class="card">
                <img src="<%- site.rootPath %>/img/<%- page.name %>/<%- data.id %>/1.jpg">
                <div class="card-img-overlay">
                  <h1 class="card-title"><%- data.name %></h1>
                  <p class="card-text"><%- (data.tag == 'works') ? data.work : data.date; %></p>
                </div>
                <a href="<%- site.rootPath %>/<%- page.name %>/<%- data.id %>.html" class="stretched-link"></a>
              </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </main>
    <%- include('partial/_footer'); %>
  </body>
</html>
